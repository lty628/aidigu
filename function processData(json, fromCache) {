    var data = json.data.data;
    
    if (fromCache) {
        // 如果是缓存数据，直接处理
        renderData(data, json);
        // 添加缓存数据加载后的滚动位置恢复
        setTimeout(function() {
            CacheManager.restoreScrollPosition();
        }, 50); // 短暂延迟确保DOM已更新
        canLoad = true;
    } else {
        // 如果是新数据，正常处理流程
        renderData(data, json);
        
        if (data.length < 8) {
            $("#showMore").hide()
            return
        }
        
        canLoad = true;
    }
}```

这个修复通过在缓存数据加载后添加滚动位置恢复的代码，确保在使用缓存数据时也能正确恢复用户之前的滚动位置。使用短暂的 `setTimeout` 确保DOM渲染完成后再执行滚动操作，这样可以获得更准确的滚动效果。